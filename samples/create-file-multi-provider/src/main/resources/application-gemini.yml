# Gemini CLI configuration
# Requires: Gemini CLI installed (npm install -g @anthropic-ai/gemini-cli)
# Auth: GOOGLE_API_KEY or GEMINI_API_KEY env var
spring:
  ai:
    agents:
      gemini:
        model: gemini-2.5-flash
        timeout: PT2M
